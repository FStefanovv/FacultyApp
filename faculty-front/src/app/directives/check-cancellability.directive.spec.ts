import { CheckCancellabilityDirective } from './check-cancellability.directive';

describe('CheckCancellabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckCancellabilityDirective();
    expect(directive).toBeTruthy();
  });
});
